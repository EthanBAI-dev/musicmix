# 技术博客

三篇都以**负结果**或**方法学教训**为主。项目里做的正向改动只有两个，
而搞清楚"另外五个为什么不行"花的时间更多、也更值得写。

| 文章 | 一句话 |
|---|---|
| [01 · 换 30 秒窗口的选法，胜过换一个大 3.35 倍的模型](01-窗口比模型更重要.md) | 数据加载里的 `clip_seconds=30` 和 `offset=中间` 是整轮实验里效应最大的两个变量，而它们从没被当作变量 |
| [02 · 多标签分类里，调阈值比换模型有用得多](02-调阈值比换模型有用.md) | +0.126 Macro-F1，不改模型、不重训、不加参数；而专为不平衡设计的 ASL 损失反而有害 |
| [03 · 先分离再打标签，真的更准吗？](03-先分离再打标签真的更准吗.md) | 3.6 小时算力换来两个假设双双证伪，以及一档专门为了让负结果站得住而设的零参数对照 |

所有数字来自 5 种子配对比较，判据是**方向一致性 k/n**，脚本与原始结果见 `results/`。
